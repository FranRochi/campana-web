name: Deploy Frontend

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Deploy on server
        run: |
          chmod +x ~/apps/campana/deploy.sh
          ~/apps/campana/deploy.sh
